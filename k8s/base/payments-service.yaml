apiVersion: v1
kind: Service
metadata:
  name: payments-service
  namespace: default
spec:
  selector:
    app: payments-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
