apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: default
spec:
  selector:
    app: notifications-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8003
  type: ClusterIP
