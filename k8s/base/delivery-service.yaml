apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: default
spec:
  selector:
    app: delivery-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
